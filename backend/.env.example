PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/foodie
CLIENT_URL=http://localhost:5173

# Optional admin key (at least 32 characters); keep server-side only.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Experiment 6: set a random secret of at least 32 characters
JWT_SECRET=
GOOGLE_CLIENT_ID=
